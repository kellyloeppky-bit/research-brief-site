import { z } from 'zod';

// Re-export zod for convenience
export { z };

/**
 * Zod type provider (using fastify-type-provider-zod)
 */
export type { ZodTypeProvider } from 'fastify-type-provider-zod';
